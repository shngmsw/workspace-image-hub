services:
  hub:
    image: ghcr.io/shngmsw/workspace-image-hub:latest
    env_file: .env
    ports:
      - "3000:3000"
    volumes:
      - hub-data:/data
    restart: unless-stopped

volumes:
  hub-data: {}
